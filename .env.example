# Copy to .env (never commit .env). Loaded automatically by the scripts via python-dotenv.
ANTHROPIC_API_KEY=sk-ant-...
# Tinker (only for --sampler tinker)
TINKER_API_KEY=...
# vLLM / any OpenAI-compatible server (only for --sampler vllm)
VLLM_BASE_URL=http://localhost:8000/v1
VLLM_API_KEY=EMPTY
